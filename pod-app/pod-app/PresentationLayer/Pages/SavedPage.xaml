<Page x:Class="pod_app.PresentationLayer.Pages.SavedPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:views="clr-namespace:pod_app.PresentationLayer.Views"
      mc:Ignorable="d"
      Title="SavedPage">

    <Grid Background="#0F0F10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP BAR -->
        <Grid Grid.Row="0" Margin="20,20,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="Tillbaka"
                    Click="BtnBack_Click"
                    Width="110" Height="36"/>

            <TextBlock Grid.Column="1"
                       Text="Sparade poddar"
                       Foreground="White"
                       FontSize="20"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="2"
                    Content="Filtrera"
                    Width="110" Height="36"/>
        </Grid>

        <!-- CONTENT -->
        <Border Grid.Row="1"
                Margin="40"
                Padding="20"
                Background="#1A1A1D"
                CornerRadius="14">

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="PodListControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0 10">

                                <!-- FEED TILE -->
                                <views:PodFeedView MouseLeftButtonUp="OnFeedClicked"/>

                                <!-- EPISODES (EXPAND AREA) -->
                                <ItemsControl ItemsSource="{Binding Episodes}">
                                    <ItemsControl.Style>
                                        <Style TargetType="ItemsControl">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ItemsControl.Style>

                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <views:PodFeedView/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

        </Border>
    </Grid>
</Page>
